import * as React from 'react';
import { Header as StyledHeader } from '../styles/Header';

const Header = () => (
  <div>
    <StyledHeader>Query Builder</StyledHeader>
  </div>
);

export default Header;
